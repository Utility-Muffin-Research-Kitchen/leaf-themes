# Reviewed theme versions

Each published theme version has a `themes/<id>/<version>.json` file here, added by the
review pull request the submission bot opens. Merging that pull request publishes the
version. The files record the sha256 and sizes of the published zip and preview, the
license, the owner and the submission issue. The theme files themselves live in this
repository's releases, never in git.
